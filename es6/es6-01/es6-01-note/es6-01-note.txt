## var
 * 1、可以重复声明
 * 2、不能定义常量
 * 3、不支持块作用域
## let
    let:es6中子增加的两种变量声明的方式，可以解决以前var的一些问题 
    1、不能重复声明
    -- 错误提示：Uncaught SyntaxError: Identifier 'a' has already been declared
    -- 变量a不能重复声明
   2、可以定义常量，常量不可以更改
   3、支持块作用域
   * */
-- 以前JS只有两个作用域  一个全局  一个函数级


## 模板字符串
-- ``：反引号 
    字符串可以嵌套变量 ${变量}
    模板字符串可以折行
-- startsWith: 判断字符串以什么开头,返回布尔值
-- endsWith: 以什么结束,返回布尔值
-- includes：判断一个字符串是否包含另一个字符穿
-- repeat: 重负3次
-- padStart: 补充多少位, 参数1：多少位，参数2：补的内容
-- padEnd
-- 数组求和：reduce 计算 汇总 收敛
-- 展开运算符

## 箭头函数
-- 箭头函数没有自己的this，他会使用上层this

## 数组
-- 常用的数组方法
-- map：数组映射
-- reduce：
-- reduceRight
-- filter：过滤，返回一个新数组
-- forEach：循环遍历
-- some: 满足一个条件就行，返回布尔值
-- find: 查找、只会返回一个元素
-- findIndex：返回对应的索引
-- every：要求每个元素都要满足条件
-- from：把一个类数组变成一个数组
-- of：创建一个为3的数组

## 对象
-- 如果对象的属性名和变量名如果一直的话可以二合一
-- super:可以调用父级的方法

## 类

